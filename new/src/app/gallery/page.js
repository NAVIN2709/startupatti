import GalleryClient from "./GalleryClient";

export const metadata = {
  title: "Gallery | Startup Atti",
  description:
    "Memorable moments from previous Startup Atti events in Chennai. Glimpses of networking, learning, and founder stories.",
};

export default function GalleryPage() {
  return <GalleryClient />;
}
